You are working in a ROS 2 Jazzy Python package:

~/ros2_ws/src/drone_tracking_demo

Current branch:
wind-disturbance-module

The system currently works:
- drone_simulator.py supports wind_level internally, currently likely as self.wind_level = "strong" or similar.
- full_demo.launch.py launches drone_simulator, mpc_tracker, robot_state_publisher, and RViz.
- MPC tracking, URDF, TF, RViz, and metrics all work.

Task:
Make wind_level configurable from the ROS 2 launch command.

Required behavior:
The user should be able to run:

ros2 launch drone_tracking_demo full_demo.launch.py wind_level:=none
ros2 launch drone_tracking_demo full_demo.launch.py wind_level:=mild
ros2 launch drone_tracking_demo full_demo.launch.py wind_level:=moderate
ros2 launch drone_tracking_demo full_demo.launch.py wind_level:=strong
ros2 launch drone_tracking_demo full_demo.launch.py wind_level:=extreme

A. Modify drone_tracking_demo/drone_simulator.py

1. Add a ROS parameter named:
   wind_level

2. Declare it in __init__:
   self.declare_parameter("wind_level", "strong")

3. Read it:
   self.wind_level = self.get_parameter("wind_level").get_parameter_value().string_value

4. Validate supported values:
   ["none", "mild", "moderate", "strong", "extreme"]

5. If invalid, log a warning and fall back to "strong".

6. Log at startup:
   Wind level set to: <wind_level>

7. Preserve all existing behavior:
   - subscribe /drone/cmd_vel
   - publish /drone/odom
   - publish TF world -> drone_body
   - first-order velocity response
   - compute_wind_disturbance()
   - wind logging every 2 seconds

8. Do not modify mpc_tracker.py or path_tracker.py.

B. Modify launch/full_demo.launch.py

1. Add launch argument:
   wind_level
   default_value="strong"

2. Pass this argument as a parameter to drone_simulator:
   parameters=[{"wind_level": wind_level_config}]

3. Preserve existing launch behavior:
   - simulator
   - mpc_tracker
   - robot_state_publisher
   - rviz2

4. Do not break URDF loading.

C. Syntax checks

Run:
python3 -m py_compile src/drone_tracking_demo/drone_tracking_demo/drone_simulator.py
python3 -m py_compile src/drone_tracking_demo/launch/full_demo.launch.py

D. Return:
- files changed
- exact launch commands for no-wind and strong-wind tests
